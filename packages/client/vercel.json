{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @collabboard/shared build && pnpm --filter @collabboard/client build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
